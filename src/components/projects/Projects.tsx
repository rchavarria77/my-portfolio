// Components
import { SectionContainer } from '@/components/global-components';

export const Projects = () => {
  return (
    <SectionContainer id='projects' customClass='min-h-screen'>
      [Projects Section]
    </SectionContainer>
  );
};
