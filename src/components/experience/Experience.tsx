// Components
import { SectionContainer } from '@/components/global-components';

export const Experience = () => {
  return (
    <SectionContainer id='experience'>[Experience Section]</SectionContainer>
  );
};
